import React from 'react';
import { AntDesign } from '@expo/vector-icons';

export const PlusIcon = ({ color, size, style }) => {
  return (
    <AntDesign name="plus" size={size} color={color} style={style} />
  );
};

